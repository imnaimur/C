#include <stdio.h>
#include <stdbool.h> //this header file enable us to use bool type and true fals option
int main()
{
    bool is_logged_in =true;
    if (is_logged_in) {
        printf("User Logged In\n");
    } else {
        printf("User Logged Out\n");
    }
} 
//boolean also can be represented by intiger